# =============================================================================
# CodeBuild ビルド用 buildspec
# 
# このファイルでは以下の処理を実行します：
# 1. ECRにログイン
# 2. Dockerイメージをビルド
# 3. ECRにイメージをプッシュ
# 4. ビルド結果をJSONファイルで出力（デプロイステージで使用）
# 
# 環境変数はCodeBuildプロジェクトで設定され、
# コミットハッシュベースのタグでイメージを管理
# =============================================================================

version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com
      - REPOSITORY_URI=$AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$IMAGE_REPO_NAME
      - COMMIT_HASH=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
      - IMAGE_TAG=${COMMIT_HASH:=latest}
      
  build:
    commands:
      - echo Build started on `date`
      - echo Building the Docker image...
      - docker build -t $IMAGE_REPO_NAME:$IMAGE_TAG .
      - docker tag $IMAGE_REPO_NAME:$IMAGE_TAG $REPOSITORY_URI:$IMAGE_TAG
      
  post_build:
    commands:
      - echo Build completed on `date`
      - echo Pushing the Docker image...
      - docker push $REPOSITORY_URI:$IMAGE_TAG
      - echo Writing build output file...
      - printf '{"image_uri":"%s","image_tag":"%s","commit_hash":"%s"}' $REPOSITORY_URI:$IMAGE_TAG $IMAGE_TAG $COMMIT_HASH > build_output.json
      - cat build_output.json

artifacts:
  files:
    - build_output.json